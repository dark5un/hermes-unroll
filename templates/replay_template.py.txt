#!/usr/bin/env python3
"""
Reproducible Agent Run - session $SESSION_ID
Generated by hermes-unroll v0.2.0 at $TIMESTAMP

Flags:
  python {file}               Default: dry-run from cache
  python {file} --live        Execute real LLM calls
  python {file} --from=N      Start from step N (inclusive)
  python {file} --to=N        Stop at step N (inclusive)
  python {file} --stop-at=N   Pause after N step nodes
  python {file} --substitute-tool=N '{"args": ...}'
  python {file} --show-state  Print state after each step node
  python {file} --diff other.py  Compare with another trace
  python {file} --edit '...'  Counterfactual: edit + re-execute
"""

import argparse
import json
import os
import sys
import time
from datetime import datetime

# ---- Metadata ----
# Generated: $TIMESTAMP
# Session: $SESSION_ID
# Model: $MODEL
# Provider: $PROVIDER
# LLM calls: $LLM_NL
# Tool calls: $TOOL_TC
# Total events: $EVENT_N
# Messages: $MSG_CNT
# Original duration: $DUR_STR

SESSION_ID = $JSID
MODEL = $JMODEL
PROVIDER = $JPROV
STARTED_AT = $JSTARTED
SYSTEM_PROMPT = $JSYS
ORIGINAL_DURATION_MS = $TOTAL_DUR
EXPECTED = $JEXP
TIMELINE = $TIMELINE_JSON
USAGE = $JUSAGE
REASONING_BLOCKS = $JRB
RESPONSE_CACHE = $JCACHE

$REPLAY_FUNC

$PARSE_ARGS_FUNC

$HUMAN_DUR_FUNC

if __name__ == "__main__":
    args = _parse_args()
    if args.diff:
        other_timeline = None
        try:
            other_globals = {}
            with open(args.diff) as f:
                exec(f.read(), other_globals)
            other_timeline = other_globals.get("TIMELINE", [])
        except Exception as e:
            print(f"Error loading other trace: {e}")
            sys.exit(1)
        if other_timeline == TIMELINE:
            print("✓ Traces are identical")
        else:
            our_kinds = [(e.get("kind"), e.get("offset_ms", 0)) for e in TIMELINE]
            their_kinds = [(e.get("kind"), e.get("offset_ms", 0)) for e in (other_timeline or [])]
            for i in range(max(len(our_kinds), len(their_kinds))):
                o = our_kinds[i] if i < len(our_kinds) else ("", "")
                t = their_kinds[i] if i < len(their_kinds) else ("", "")
                if o != t:
                    print(f"  Step {i}: ours {o[0]}@{o[1]}ms vs theirs {t[0]}@{t[1]}ms")
        sys.exit(0)
    if args.edit:
        print(f"Edit mode: {args.edit} (counterfactual stub)")
    result = replay(from_step=args.from_step, to_step=args.to_step)
    if args.stop_at is not None:
        steps = result.get("steps", [])[:args.stop_at]
        print(json.dumps(steps, indent=2))
    if args.show_state:
        print(f"State: {len(result.get('steps', []))} steps, {len(result.get('messages', []))} messages")
    print()
    print("=== STRUCTURED OUTPUT ===")
    print(json.dumps(result, indent=2, ensure_ascii=False))
    sys.exit(0)
